-- 0001_initial.sql
-- This project currently creates/ensures feature tables at runtime (legacy init_db).
-- Recommended next step: move all CREATE TABLE IF NOT EXISTS statements here.
--
-- Keeping this file non-empty so the migrations pipeline is in place.
SELECT 1;
